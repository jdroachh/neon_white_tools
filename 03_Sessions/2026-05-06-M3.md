# Session 2026-05-06 — M3

## What landed

### Backend (bridge.py)
- Imported `CHAPTERS`, `WHOLE_GAME_LEVELS` from `rush_data`
- Added `_load_config` / `_save_config` helpers (reads/writes `neonwhite_config.json` at project root)
- Added `_emit_to(handler, data)` — generalized event emitter (existing `_emit` kept for finder)
- **Config methods:** `get_config`, `save_config_field`
- **Steam methods:** `init_steam` (calls `steam_api.init_steam`, starts 100ms `RunCallbacks` daemon, saves dll_path to config), `get_steam_status`, `pick_dll_file`
- **Metadata methods:** `get_levels` (121 levels), `get_chapters` (15 chapters)
- **Leaderboard ops:** `run_global_export`, `run_level_search`, `run_player_lookup`, `stop_leaderboard` — all fire-and-forget with shared `_lb_stop_event`; stream rows via `_nwGlobalEvent` / `_nwLevelEvent` / `_nwPlayerEvent`

### Frontend (4 new pages)
- **Settings.jsx** — DLL path input + Browse (native file dialog via `pick_dll_file`), Connect button, status feedback, auto-saves path on blur
- **GlobalExport.jsx** — entry count, run/stop, progress bar (per-level), streaming results table (rank/level/player/time), copy button
- **LevelSearch.jsx** — level select (121 options), entry count, run/stop, streaming results table, copy
- **PlayerLookup.jsx** — Steam ID input + "Mine" button (uses `get_steam_status`), mode seg (level/chapter/game), conditional level/chapter select, streaming results with player name header, copy

### Wiring
- `api.js` — new exports: `getConfig`, `saveConfigField`, `initSteam`, `getSteamStatus`, `pickDllFile`, `getLevels`, `getChapters`, `runGlobalExport`, `runLevelSearch`, `runPlayerLookup`, `stopLeaderboard`
- `shared.jsx` Sidebar — now accepts `steamReady` + `playerName` props; footer shows dot color (ok/bad) + player name or "Not connected"; Settings nav item highlights when active
- `main.jsx` — adds `steamStatus` state (polled on mount via `getSteamStatus`), imports + mounts all 4 new pages, passes `onSteamConnected` to Settings

## Verified
- Frontend builds clean
- Bridge smoke test: `ping`, `get_rushes`, `get_levels` (125 entries), `get_chapters` (15), `get_steam_status` all correct
- 5/5 Python tests passing
