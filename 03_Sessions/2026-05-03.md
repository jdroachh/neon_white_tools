# Session 2026-05-03

## What was done
- Initial setup session
- Read SETUP_INSTRUCTIONS.md and performed full codebase reconnaissance
- Created vault folder structure: 00_Inbox, 01_Codebase_Map, 02_Decisions, 03_Sessions
- Wrote overview.md, architecture.md, and glossary.md based on code exploration
- Created CLAUDE.md with startup ritual

## Key findings
- Main app: SteamScraper/neonwhite_app.py (2991 lines, tkinter GUI)
- Scrapes Neon White leaderboards via Steam Steamworks API (ctypes → steam_api64.dll)
- Rush Tools suite for speedrun seed analysis; Seed Finder uses a compiled C extension (shuffle.dll)
- Google Sheets push feature with OAuth2 credentials bundled in the EXE (potential security concern)
- Rush Timer tab is incomplete (placeholder pass at line 1695)
- Several legacy standalone scripts in root that predate the main app

## Open questions / next steps
- No immediate tasks — waiting for user to drop items into 00_Inbox/

## Rush Seed Finder bug fixes (same day)

Headless test (`SteamScraper/_seed_finder_test.py`) confirmed three bugs and surfaced a UX issue. All four fixed in `neonwhite_app.py`:

1. **Workers ran pure-Python shuffle** (lines 400, 460). On Windows-spawn child processes, `_SHUFFLE_LIB` stayed `None` because `_load_c_shuffle()` is only called from `NeonWhiteApp.__init__`. Fix: call `_load_c_shuffle()` at the top of `_seed_search_worker`. Verified speedup: **29k → 76k seeds/sec per worker**.
2. **Progress bar never moved during search** (manager loop, ~line 1303). Old code computed `done_workers / num_cores`, so progress was 0% until first chunk finished. Fix: workers now push `("progress", count)` messages every 200k seeds; manager sums `seeds_checked / TOTAL_SEEDS`. Status bar now shows `Searching... X of 2,147,483,647 seeds checked, Y found`.
3. **`urlopen()` misplaced in worker** (old lines 414–419). Each worker hit `COMMUNITY_MEDALS_URL` after finishing its chunk for no reason. Deleted.
4. **No warning for statistically infeasible searches.** Added `_expected_match_count()` helper. `_run_finder` now shows a Yes/No dialog if expected matches across the 2.1B range is < 10. For (96 levels, 12 targets, depth 25) the expected count is ~17.88, just above threshold; (96, 12, 40) gives ~19,206.

Test harness left at `SteamScraper/_seed_finder_test.py` for re-running after future changes.

## Vault audit follow-up (same day)
- Deleted stale root files: Welcome.md, SETUP_INSTRUCTIONS.md
- Added line anchors + prose lead-ins to architecture.md
- Expanded CLAUDE.md with "Vault Map" and "Working Style" sections
- Added commented usage examples to all 00_Inbox/ files
- Pushed GitHub-prep checklist into 00_Inbox/todo.md (gitignore, credentials handling, config schema doc, archive folder)
- Saved working-style feedback to memory

---

# End-of-session summary — 2026-05-03

This session ran long. Below is the executive summary so future-Claude (after `/clear`) can pick up cleanly without rereading the full log.

## What we built / fixed (8 commits on `main`)

| Commit | Summary |
|--------|---------|
| `9832482` | Initial snapshot — vault + SteamScraper. Local git only, no remote yet. |
| `6f5d677` | **Modularize step 1:** split `shuffle_lib.py` + `seed_search.py` out. Workers re-import ~170 lines instead of 2991. |
| `efde632` | **Perf:** dropped redundant `set()` in seed-finder subset check (~1.83× faster on the check itself). Bitmask attempt was a regression — see todo #2 for the lesson. |
| `a473689` | **Logging Phase 1:** new `logger.py` with rotating file handler at `SteamScraper/logs/app.log`. Converted 5 high-value silent error sites (config load, cheater fetch, medals fetch, init_steam DLL fallbacks, Sheets push). |
| `2546cef` | **Logging Phase 2:** 5 more sites (Google libs import, Steam CDLL load, font load, `_init_c_shuffle`, Sheets auth). |
| `b00ec94` | **Modularize step 2a:** extracted `rush_data.py` (LEVELS, RUSH_LEVELS, RUSH_ALIASES, STANDARD_MEDAL_DATA, CHAPTERS — all static read-only data). |
| `b065e82` | **Modularize step 2b:** extracted `steam_api.py` (Option A — module-level globals, callers use attribute access). Most invasive change; required prefixing 14+ call sites. **Lesson learned:** missed 5 direct DLL access sites (`steam.SteamAPI_X(...)`) — saved as memory `feedback_global_extraction.md`. |
| `f8cee5c` | **Modularize step 3:** extracted `sheets.py` with **lazy import** pattern. Measured 217ms faster app launch for users who don't use Sheets. |

Also fixed in `9832482` (initial snapshot, before this session's commits): four Rush Seed Finder bugs from earlier in the day — workers weren't loading the C shuffle DLL, progress bar didn't update, misplaced `urlopen` in worker, no probability warning. All verified via the headless test harness (`SteamScraper/_seed_finder_test.py`).

## Current architecture

```
neonwhite_app.py    ~2240 lines  GUI orchestrator + per-tab runners
steam_api.py         ~250        Steamworks API (ctypes), globals, cheater list
sheets.py            ~110        Google Sheets push (LAZY-IMPORTED — see access pattern)
rush_data.py         ~265        Static level + medal data
seed_search.py        ~50        Seed finder worker (slim, no tkinter import)
shuffle_lib.py       ~110        C-accelerated Fisher-Yates shuffle
logger.py             ~70        Rotating file logger
```

`neonwhite_app.py` was 2991 lines at session start; ~25% reduction.

**Key access patterns to remember:**
- `steam_api`: `import steam_api` then `steam_api.steam_ready` etc. Never `from steam_api import steam_ready` (captures pre-init value).
- `sheets`: `import sheets` *inside* `auth_worker` / `push_worker` only. Never at module top — defeats the 217ms lazy-import win. `TOKEN_FILE = "token.json"` is duplicated in `neonwhite_app.py` for UI status checks (intentional, commented).
- `rush_data`: regular `from rush_data import ...` — pure constants.
- `seed_search` / `shuffle_lib`: deliberately slim. Don't add heavy imports here; workers re-import these on every spawn.

## What's next — pick up here

In rough priority order. Each is independently sensible to start with.

### Bigger items
- **Modularize step 4: UI section split** (todo's broader plan). The remaining ~2240 lines of `neonwhite_app.py` are mostly per-tab `_build_*` and `_run_*` methods sharing `self.*` state. Two design options to decide between *before* touching code: (a) explicit `app` parameter passed to plain functions, or (b) mixin classes. Each has tradeoffs; this is its own focused session.
- **API efficiency work** (todo "Steam API call efficiency / rate-limit safety", items 1-3): handle cache, self-imposed rate limit, exponential backoff. Best done as additions inside `steam_api.py`. User explicitly cares about this — burn rate / rate-limit safety.

### Medium items
- **Investigate "Standard Medals" tab** in `00_Inbox/questions.md` — `_build_rush_std`/`_run_std` exist in code but user reports no visible tab. Either dead code or wired under different name. Affects accuracy of `01_Codebase_Map/overview.md`.
- **Logger Phase 3** — sweep remaining `except Exception: pass` sites (todo #8 says Phase 1+2 are done; some lower-priority sites remain).
- **GitHub-prep checklist** in `00_Inbox/todo.md`: document `neonwhite_config.json` schema; decide what to do with bundled `credentials.json` in PyInstaller spec; possibly archive `Neon White App versions/` and standalone scripts.

### Small items
- **Polish:** silence the OAuth URL print in `sheets.get_sheets_service` (one-line change, in todo).
- **`6f5d677` commit message has a typo** ("our of neoonwhite_app"). Cosmetic; in history now.

### Bigger-picture (user's notes in `00_Inbox/ideas.md`)
- "Rewrite entire app using Opus" — open-ended, worth scoping.
- "Google Auth alternatives" — current flow bundles `credentials.json` in EXE (security concern). Options: device-code OAuth, user-supplied service account JSON, or drop Sheets in favor of CSV-only.

## Test / verification entry points

- **Headless seed finder test:** `cd SteamScraper && python _seed_finder_test.py` — covers shuffle correctness, worker DLL loading, progress cadence, probability helper, subset-check benchmark.
- **App launch:** `cd SteamScraper && python neonwhite_app.py` — needs Steam running for full functionality; `import neonwhite_app` alone is enough to verify imports.
- **Logs to check on errors:** `SteamScraper/logs/app.log` (rotating, 5MB × 3).

## Saved memories that affect future sessions

(All in `~/.claude/projects/E--Claude-Neon-White-App/memory/`.)
- `project_neonwhite.md` — project context (what this app is)
- `feedback_working_style.md` — terseness, confirm before SteamScraper edits, don't restructure code unprompted
- `feedback_git_commands.md` — Claude runs git directly but always confirms exact commands first; pause before destructive ops
- `feedback_subagent_strategy.md` — default to Explore for cross-file searches, Plan for design questions, general-purpose for multi-file research
- `feedback_global_extraction.md` — when extracting globals, grep BOTH `\bNAME\b` AND `\bNAME\.` (attribute access). Lesson from steam_api extraction.

## Known caveats / footguns for next session

- **No remote yet.** Local-only git. Don't push without setting up a remote and revisiting the GitHub-prep checklist (especially `credentials.json` exposure in EXE).
- **Worker spawn cost is precious** — keep `seed_search.py` and `shuffle_lib.py` import-light. Don't add `from logger import` etc. there. Multi-process `RotatingFileHandler` write contention is also a real concern if logger ever gets wired into workers.
- **Five-character footgun:** stray `steam_api.steam_api.init_steam` in a doc-comment was caused by `replace_all` of `init_steam(`. Already fixed but worth remembering — when using `replace_all`, check if the search string appears in YOUR OWN comments first.
