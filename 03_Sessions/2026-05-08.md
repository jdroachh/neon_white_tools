# Session 2026-05-08

## Ghosts library wired ✓

### What shipped

- **`SteamScraper/webview_app/resources.py`**: replaced `_GHOSTS_SHEET_ID` placeholder with real sheet ID (`1FXr-2Rs4RgPF6Oo2BVz-_UQXf_3PPa8nCGstdIl12q8`). No other changes — `_index_ghosts` already matched the schema.
- **`tools/build_ghosts_sheet.py`**: new one-shot dev walker script. OAuths against Drive readonly + Sheets write (separate token cached at `tools/.ghosts_token.json`). Walks `Ghosts/<Chapter>/<Level>/<Medal>/<Player> - <Time>/` tree, strips `1-1 ` / `V-1 ` / `R-1 ` / `Y-1 ` prefixes from level folder names, validates against `rush_data.LEVELS`, writes `level, medal, player, time, drive_url` rows sorted by (LEVELS order, medal tier, time). Full-tab overwrite — idempotent.
- **`.gitignore`**: added `tools/.ghosts_token.json` and `tools/__pycache__/`.
- **`CHANGELOG.md`**: created; covers M1–M4 + all recent features.

### Run result

```
Walked: 472 ghost files found, 2 warning(s).
Done. 472 rows written across 121 levels.
```

Two skipped entries (no `.phant` file inside folder): `hacatu - 1-08.497` (The Third Temple/Emerald) and `JdRoach - 14.649` (Canals/Amethyst). Known Drive-side gaps, not script bugs.

### Fixes during development

- Drive API was not enabled in the GCP project — enabled via console, no code change.
- Initial regex `^\d+-\d+\s+` only stripped numeric prefixes; extended to `^(?:\d+|[A-Za-z])-\d+\s+` to also handle Violet/Red/Yellow `V-1 ` / `R-1 ` / `Y-1 ` prefixes.

### Verified in-app

- Resources → Ghosts → level + medal picker loads rows ✓
- "Open in browser" opens Drive file page ✓
- Empty level/medal combo shows empty-state message ✓
- `logs/app.log` shows `Ghosts loaded: N stages indexed` (no longer the old "Could not load" message) ✓
- No rebuild required — `resources.py` is runtime Python, sheet ID takes effect immediately ✓

## Accent color picker ✓

### What shipped

- **`SteamScraper/webview_app/bridge.py`**: added `"accent_color": "#00e09a"` to `_DEFAULT_CONFIG` — falls back cleanly if key missing from JSON.
- **`frontend/src/api.js`**: added `export function applyAccent(hex)` — sets `--accent` on `document.documentElement`.
- **`frontend/src/main.jsx`**: imports `applyAccent`; applies saved `accent_color` from config on boot (inside existing `getConfig()` call).
- **`frontend/src/pages/Settings.jsx`**: new `accentColor` state + `handleAccentPick`; added "Color Picker (Restrain setting)" `Field` with 8 preset swatches (28px rounded squares); selected swatch gets white border + color outline ring. Help text added to right panel. Medal/chart colors untouched.

### Palette

Mint `#00e09a` · Cyan `#22d3ee` · Sky `#38bdf8` · Violet `#a78bfa` · Magenta `#f472b6` · Orange `#fb923c` · Amber `#fbbf24` · Rose `#fb7185`

### Build

`npm run build` clean.

## World Record VODs page ✓

### What shipped

- **`SteamScraper/webview_app/resources.py`**: added `_WR_SHEET_ID` / `_WR_TAB` constants, `_WRS` cache dict, `wrs_loaded` in `_STATUS`, `_index_wrs()` (csv.reader-based, positional zip against `rush_data.LEVELS`, canary checks on Movement/Sacrifice/Rocket, raises ValueError on structural mismatch so the caller leaves `wrs_loaded=False`), extended `_fetch_resources_bg()` with a third try/except block, and `get_wr_for(level, platform) -> dict | None`.
- **`SteamScraper/webview_app/models/resources.py`**: added `WorldRecordRow` Pydantic model; added `wrs_loaded: bool = False` to `ResourcesStatus`.
- **`SteamScraper/webview_app/bridge.py`**: added `get_world_record(level, platform)` bridge method.
- **`frontend/src/api.js`**: added `getWorldRecord(level, platform)` wrapper.
- **`frontend/src/shared.jsx`**: added `trophy` SVG icon; added `wrs` entry to `NAV_ITEMS.resources`.
- **`frontend/src/main.jsx`**: imported `WorldRecordVods`, added `wrs` to `PAGE_TITLES` and `RES_PAGES`.
- **`frontend/src/pages/WorldRecordVods.jsx`**: new page — level dropdown + PC/Switch/PlayStation `Seg` → single WR card (Runner, Time, Date, Watch button, title subtitle). Graceful empty-state for missing platforms (e.g. Rocket + PlayStation).

### Implementation notes

- Column offsets: PC=2, Switch=10, PlayStation=18. Within each block: +0 name, +2 time_formatted, +3 date, +4 youtube_url, +5 title.
- Canary check uses PC Video Title (col 7). ValueError on mismatch leaves `wrs_loaded=False`; page shows "Resources not loaded." rather than partial/misaligned data.
- Platforms with blank `youtube_url` are silently skipped; non-YouTube URLs get a warning + skip.
- `open_external_url` allow-list already covered YouTube — no change needed.

### Build + tests

`npm run build` clean. `pytest tests/` — 38/38 passed.

## What's next

- `neonwhite.spec` cleanup (drop `credentials.json` + `steam_api64.dll` from bundle, switch entry point to `webview_app/main.py`, add `frontend/dist` data)
- Drive folder data fixes: add `.phant` files to the two skipped entries when available
- Re-run `tools/build_ghosts_sheet.py` whenever new ghosts are added to Drive
