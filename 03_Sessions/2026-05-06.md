# Session 2026-05-06

## Headline

M1 groundwork: pywebview scaffold + Pydantic models + frontend build pipeline.

## What landed

### New packages

**`SteamScraper/webview_app/`** — bridge layer package:
- `__init__.py`, `bridge.py` (`JsApi` with `ping()`), `main.py` (pywebview bootstrap)
- `progress.py`, `steam_runtime.py` — M2/M3 stubs with docstrings
- `models/` — full Pydantic model set for all 5 domains: `seed.py`, `splits.py`, `timer.py`, `leaderboard.py`, `settings.py`

**`frontend/`** — esbuild-based JSX pipeline:
- `package.json` (esbuild + react + react-dom devDeps)
- `esbuild.config.mjs` (replaces vite; bundles `src/main.jsx` → `dist/bundle.js`, copies `src/index.html` → `dist/index.html`)
- `src/index.html` — lifted from Hi-Fi HTML with Babel-in-browser removed, `bundle.js` wired
- `src/main.jsx` — M1 smoke-test page: calls `ping()` on mount, shows bridge status

**`tests/test_bridge.py`** — 10 tests, all passing:
- `test_ping` — `JsApi().ping()` returns `{ok: True, version: ...}`
- One round-trip test per model domain (seed, splits, timer, leaderboard, settings)

**`01_Codebase_Map/webview_app.md`** — bridge layer docs per CLAUDE.md curation rule.

## Verified gates

- `pytest tests/test_bridge.py` → 10 passed
- `cd frontend && npm run build` → `dist/bundle.js` + `dist/index.html` produced
- `webview` + `pydantic` both import on Python 3.14.4
- All `webview_app` submodules import cleanly

## Python 3.14 note

pythonnet has no wheel for Python 3.14 yet (build fails). pywebview was installed `--no-deps` + `bottle`/`proxy_tools` manually. The `webview` import succeeds. The actual window won't open until pythonnet is available or pywebview releases a 3.14-compatible backend — **to be verified when the user runs `python -m SteamScraper.webview_app.main`.**

## Next

M1 wiring pass — real Seed Parser wired to `shuffle_lib.full_shuffle`. Blocked on user answers to 6 open questions (see plan §6). Stop here and wait.

## Open questions (from plan §6)

1. `HEALTHPACK_LEVELS` authoritative source?
2. Standardize canonical orders per rush — in `rush_data.py` or new data?
3. `state.sampleSeedsOverride` — keep or strip?
4. Settings location — `neonwhite_config.json` or `platformdirs`?
5. Hell Rush spacing score formula (0-100)?
6. Theme switching — pure-CSS or backend-aware?

---

## Session continuation — Q1 + Q5 resolved

### What landed

- `data/healthpacks.json` — authoritative 11-level HP list (Glass Port, Jumper, The Clocktower, Apartment, Fuse, Estate, Ricochet, Fortress, The Third Temple, Congregation, Absolution)
- `webview_app/hell_rush.py` — `score_hell_rush(level_order)` implemented per user-specified formula; verified: seed 712788 → 77 (matches mockup sample data exactly)
- `02_Decisions/2026-05-06-hell-rush-scoring.md` — decision record
- Removed TODO(M1-Q5) from `models/seed.py`, TODO(M1-Q2) from `models/splits.py`

---

## Session continuation — M1 wiring

### What landed

**Backend (`webview_app/bridge.py`):**
- `parse_seed(rush_name, seed)` — calls `full_shuffle`, returns ordered level names
- `reorder_splits(rush_name, seed, gold, segments)` — splits updater logic from `tab_rush_splits`
- `standardize_splits(rush_name, seed, gold, segments)` — inverse, from `tab_rush_std`
- `get_rushes()` — returns rush metadata list
- Rush name → key/count mapping extracted from `neonwhite_app._rush_key_*`

**Frontend (`frontend/src/`):**
- `styles.css` — copied from `ClaudeDesignHandoff/hifi-styles.css`
- `api.js` — `waitForApi()` + wrappers for all 5 bridge methods
- `shared.jsx` — `Icon`, `Titlebar`, `Sidebar`, `PageHead`, `Field`, `Seg`, `Cb`, `Btn`, `RushSelect`, `OutputPanel`, `ErrorBanner`; Sidebar nav is now clickable
- `pages/SeedParser.jsx` — wired: form → `parseSeed()` → 4-column level grid
- `pages/SplitsUpdater.jsx` — wired: form → `reorderSplits()` → output panels
- `pages/Standardize.jsx` — wired: form → `standardizeSplits()` → output panels
- `main.jsx` — full app router (state-based); dark theme default; Placeholder for unimplemented pages

**Verified:** standardize(reorder(gold)) === gold (round-trip test). Seed 12345 parse matches tk app. pywebview window opens with working pages.

### All 6 open questions resolved

Q1 ✓ HP list confirmed (11 levels, user-supplied)  
Q2 ✓ Standard order = RUSH_LEVELS in rush_data.py, no new data needed  
Q3 ✓ Strip sampleSeedsOverride on M2 wire-up  
Q4 ✓ Stay with neonwhite_config.json through M3  
Q5 ✓ Formula implemented and verified  
Q6 ✓ Pure CSS, persist theme string in Settings
