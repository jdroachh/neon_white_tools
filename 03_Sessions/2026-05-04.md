# Session 2026-05-04

## Headline

Modularize step 4 — UI section split, **complete**. Extracted all 5 Rush sub-tabs **plus** all 5 main-app tabs from `neonwhite_app.py` into one-file-per-tab mixin modules.

`neonwhite_app.py`: **2255 → 744 lines (-67% from start of session series)**. Each tab verified live in the running app.

## Setup decisions (still in force)

- Grouping: **option (b) — one file per tab area**
- Access pattern: **(ii) mixin classes**, no `app` parameter rewrites

## Extractions this session — main-app tabs

| Step | Module | Lines | core delta | Notes |
|---|---|---|---|---|
| 7 | `tab_level.py` | 146 | (already done end of last sub-session) | `LevelTabMixin`. Verified live — Level Search returns entries. |
| 8 | `tab_global.py` | 148 | 1556 → 1301 (-255) | `GlobalTabMixin` + `_browse_output_global`. Verified live — small CSV export works. |
| 9 | `tab_player.py` | 212 | 1301 → 1107 (-194) | `PlayerTabMixin`: `_build_player_section`, `_update_player_mode`, `_use_my_steam_id`, `_run_player`, `_player_worker`. Sets `self._player_results` (read by core's push-to-sheet). Verified live — single-level lookup, chapter mode swap, whole-game label all work. |
| 10 | `tab_settings.py` | 218 | 1107 → 913 (-194) | `SettingsTabMixin`: build, browse-DLL, browse-output, connect-from-settings, save-settings, extract-sheet-id, sheets-auth, sheets-signout. **Lazy-imports `save_config` from `neonwhite_app`** to avoid a circular import. **Re-duplicates `TOKEN_FILE = "token.json"`** with the same "duplicated on purpose" rationale (importing `sheets` at startup pulls slow Google libs). Verified live. |
| 11 | `tab_sidebar.py` | 185 | 913 → 744 (-169) | `SidebarTabMixin`: `_build_sidebar` + `_show_section`. Owns the section-key → frame map and nav highlighting. Highest-risk extraction since every other tab plugs into the frame map; verified live by clicking every nav item, collapsing/expanding both groups, confirming status panel renders. |

Final MRO order: `NeonWhiteApp → RushParser → RushSplits → RushStd → RushTimer → RushFinder → Level → Global → Player → Settings → Sidebar → object`.

## Pattern / lessons

- **Lazy-imports for circular deps.** `tab_settings.py` needs `save_config` (defined in core). Top-level `from neonwhite_app import save_config` would loop. In-method `from neonwhite_app import save_config` works because by the time any settings method is *called*, the core module has finished loading. Cheap, local, and only at the two real call sites. Pattern to remember if other mixins ever need a core utility.
- **TOKEN_FILE was already a "duplicated on purpose" constant** in core to avoid pulling `sheets` at startup. The same constraint applies in `tab_settings.py` — duplicated again with the same comment. Acceptable because the constant is one short string and the alternative (eager `import sheets`) would re-introduce the slow startup the previous extraction round was designed to avoid.
- **Sidebar order matters.** Every other tab's `_build_*_section` creates `self.<key>_frame`, and `_show_section` reads that map. The build order in `__init__` builds tab frames first, then `_build_sidebar` last; that order is preserved across the extraction.

## Bugs surfaced

None new this session. The three pre-existing bugs from the earlier sub-session (Seed/shuffle discrepancy, Run Timer parser, Seed Finder error paths) remain logged in `00_Inbox/todo.md`.

## Vault updates

- `01_Codebase_Map/overview.md` — added a "Module layout" table covering all extracted modules.
- `00_Inbox/todo.md` — removed the four "modularize" / "tab_level" / "option (b)" task lines (all done).

## What's next

Modularization is done. Remaining tracks from `ideas.md`:

- **API efficiency work** (caching `find_leaderboard` handles, rate limit, backoff, optional pipelining)
- **Slim worker module** for seed search (drop per-search startup time)
- **Move seed-search loop into C** ← done this session (see below)
- **Archive `Neon White App versions/`** + write a `.gitignore` before any GitHub push
- **Document `neonwhite_config.json` schema** somewhere in `01_Codebase_Map/`
- **Polish:** silence Google OAuth URL print, fix the three logged pre-existing bugs

User-mentioned but not started:
- Sub-agent config setup
- Decide on rewrite-with-Opus vs. continued incremental refactor
- Google Auth alternatives (currently bundles `credentials.json` into the EXE)

---

## Session 2 — `find_seeds_batch` C batch search (todo item #4)

### What changed

- **`compile_shuffle.py`**: added `find_seeds_batch` to `C_CODE` next to `full_shuffle`. Adds `#include <stdint.h>`. Extended `verify_dll()` with a smoke test (cross-checked against `full_shuffle` over 1k seeds). Rebuilt `shuffle.dll`.
- **`shuffle_lib.py`**: registered `find_seeds_batch` argtypes/restype; added Python wrapper that raises on missing DLL.
- **`seed_search.py`**: rewrote `_seed_search_worker` to call `find_seeds_batch` in 250k-seed slabs. Queue protocol unchanged.

### Measured speedup

- Old path: ~89k seeds/sec (ctypes allocation + `list()` round-trip + `issubset` per seed dominated)
- New path: ~1.1M seeds/sec (one ctypes boundary crossing per 250k seeds; C does the full loop)
- **~12× end-to-end speedup**
- Stop latency: ~234ms (well within 500ms budget)

### Deviations from design doc

Three design doc values needed correction:

1. **`arr[64]` → `arr[128]`**: design doc said 64, but White rush uses 96 levels. Would have silently overflowed the stack.
2. **Single `uint64_t target_mask` → `target_mask_lo` + `target_mask_hi`**: `1ULL << N` with N≥64 is UB in C; on x86 the shift wraps mod 64, causing false positives for seeds where a level with index 64+ appears in the first `depth` positions. Split into two 64-bit masks covers up to 128 levels safely.
3. **`SLAB_SIZE` 1,000,000 → 250,000**: a 1M-seed slab at 1.1M seeds/sec takes ~910ms > 500ms stop budget. Design doc noted 250k as the fallback tuning — applied it.

### Verification

- DLL smoke test passes (20 matches in seeds 0–999 cross-checked against `full_shuffle`)
- Correctness: 8-level and 96-level workers produce identical match lists to `full_shuffle`-based reference
- Stop test: PASS (234ms)
- `full_shuffle` unchanged; `tab_rush_parser`, `tab_rush_splits`, `tab_rush_std`, `tab_rush_timer` all import cleanly

---

## Session 3 — verify_dll hardening (commit 700c0d0)

Three additions to `verify_dll()` in `compile_shuffle.py`:

- **`#define MAX_LEVELS 128`**: cosmetic — replaces the hard-coded `int arr[128]` literal in `find_seeds_batch`. No behavioral change.

- **96-level hi-mask smoke test** — target `{5, 60, 70, 90}`, depth=50. Levels 5 and 60 land in `target_mask_lo`; 70 and 90 land in `target_mask_hi`. This is the substantive work: it exercises both mask halves in a single test. The prior 8-level test (Session 2) used only low-index levels, so `target_mask_hi` was never exercised — a correctness gap left by Session 2's design-doc deviation #2 (the lo/hi split). depth=50 yields ~3400 matches in 50k seeds; depth=10 gives ~3, too sparse to be useful. On failure, a diagnostic loop attributes each missing seed to the lo-mask side, hi-mask side, or both.

- **Buffer-full path test**: drives `find_seeds_batch` with `out_capacity=4`, accumulates matches across multiple slab calls until all 50k seeds are covered, then asserts `accumulated == expected_96` and `total_scanned == 50000`. The prior smoke test always allocated a buffer large enough to hold all matches in one call — the multi-slab accumulation loop in the Python wrapper had never been exercised.

Verify passes clean.

---

## Session 4 — verify_dll cleanup (commit 4d97b2a)

Two non-behavioral fixes to `compile_shuffle.py` / `verify_dll()`. Both bugs were introduced in Session 3's commit (700c0d0) — they were placeholders or oversights in the new code, not pre-existing issues.

1. **Diagnostic depth mismatch** (`arr96[:10]` → `arr96[:DEPTH_96]`): the failure-attribution loop added in Session 3 inspected only the first 10 positions of the 50-position depth window, so nearly every missing seed was wrongly counted as "both lo+hi failed". Now reads the correct slice.

2. **Redundant `set()` on issubset call**: `target_levels_96.issubset(set(arr96[:DEPTH_96]))` → `target_levels_96.issubset(arr96[:DEPTH_96])` in the 96-level smoke test's reference-building loop. `issubset` accepts any iterable; matches style already used on the 8-level path.

Verify still passes clean after both edits.
