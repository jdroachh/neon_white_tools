# Session 2026-05-07

## What landed

### YouTube error 153 — fixed (localhost HTTP server)

- Root cause: WebView2 was loading `frontend/dist/index.html` over `file://`, giving the page a null origin that YouTube's embed runtime rejects.
- Fix: `SteamScraper/webview_app/main.py` now spins up a `ThreadingHTTPServer` (daemon thread, random free port on 127.0.0.1) rooted at `frontend/dist/` before creating the pywebview window. Window URL changed from `file:///...index.html` to `http://127.0.0.1:<port>/`. ~30 lines added.
- Confirmed working by user.

### Medal target time display — off-by-1ms corrected

- `get_medal_times` in `bridge.py` returns the exclusive cutoff (e.g. 18.200s for Movement Emerald). A player needs strictly less than that value, so the max qualifying time is cutoff − 1ms.
- Fix in `frontend/src/pages/RouteVideos.jsx`: `displayTarget = targetSecs - 0.001` used in place of raw `targetSecs` for the pill display.

### YouTube iframe error handling

- Added `enablejsapi=1&origin=<window.location.origin>` to the embed URL so YouTube's iframe API can post messages back.
- `window.addEventListener("message", ...)` listens for `infoDelivery` / `onError` events from `youtube.com`; sets `videoError` state on any error.
- When `videoError` is true, the iframe is replaced with a "Could not load video in app / Open in YouTube" fallback. Error resets on row selection change.
- Column header "Title" → "Route" in the video table.
- M4 known bug section removed from `00_Inbox/todo.md`; `00_Inbox/handoff_youtube_153.md` deleted.

## Hell Rush Mode on Seed Finder

**Backend (`bridge.py`):**
- Added `from .hell_rush import score_hell_rush, HEALTHPACK_LEVELS`
- `start_finder` signature extended: `hell_rush: bool = False, hell_rush_min: str = "70"`
- Validation: non-White/Mikey + HR on → error; unparseable threshold → error
- Manager loop: scores each surviving seed via `score_hell_rush`; drops below threshold; `result` payload includes `score: int|null`; every level cell carries `is_healthpack: bool`

**Frontend (`api.js`, `SeedFinder.jsx`):**
- `startFinder` passes `hellRush`, `hellRushMin` through
- HR toggle + threshold field visible only for White/Mikey; auto-reset on rush switch
- `done` event re-sorts results score-desc when HR on
- Score pill on SeedCard when `result.score != null`; HP ♥ glyph (muted) in level cells when `lvl.is_healthpack`; coexists cleanly with target `◀` arrow

**Tests:** 5 new tests in `tests/test_bridge.py`. pytest: 27 passed. `npm run build`: clean.

## Seed Finder — healthpack heart glyph alignment

- In fullscreen the `♥` was trailing at the far right of the level row, easy to miss.
- Moved it inline immediately after the level name; bumped size 9px → 12px and saturated the color slightly (`#e05070`).
- `frontend/src/pages/SeedFinder.jsx` — committed `091f1cd`.

## Excluded Levels — Seed Finder

Mirror of "Desired Starting Levels": new toggle (White/Mikey only) that drops any candidate seed where a user-listed level appears within the first N shuffled positions.

**Bridge (`bridge.py`):**
- `start_finder` extended with `excluded_levels: str = ""`, `excluded_window: str = ""`.
- Validation reuses `_parse_level_names`; window range is `1..count-1`; non-White rush submitting either field → gating error.
- Manager-loop post-filter inserted right after the `forced_idx` check (line ~432). Zero C-kernel / `seed_search.py` changes.
- Result `level_order` cells now carry `is_excluded: bool`.

**Frontend (`api.js`, `SeedFinder.jsx`):**
- `startFinder` takes two trailing string params.
- New state (`excludedOn`, `excludedLevels`, `excludedWindow`); UI mirrors the Hell Rush / Force First `<Seg>` + `<Field>` pattern; resets on rush change.
- Result-card cells render amber (`rgba(255,180,60,*)`) when `is_excluded`. Priority chain: target > forced > excluded.

**Polish:**
- Excluded-levels placeholder → `e.g. Godspeed, Pummel`.
- Desired-starting-levels placeholder is now rush-aware via a `DESIRED_PLACEHOLDERS` map (Violet/Red/Yellow each get rush-appropriate examples; White/Mikey keeps `The Third Temple, Absolution`).

**Tests:** 4 new in `test_bridge.py` (bad name, out-of-range window, valid request, non-White rejection). **pytest: 31 passed**, `npm run build` clean.

## Order Matters? — Seed Finder (website parity)

Toggle for Violet / Red / Yellow that requires target levels to appear in the exact user-specified positions at the top of the shuffle (position 01, 02, …).

**Bridge (`bridge.py`):**
- `start_finder` extended with `order_matters: bool = False`.
- Validation: `order_matters=True` + White/Mikey → error ("Order Matters is only supported for Violet, Red, and Yellow.").
- Manager-loop post-filter after `excluded_set` check: `if order_matters and any(order[i] != target_indices[i] for i in range(len(target_indices))): continue`.
- `_parse_level_names` already preserves input order — no changes needed there.

**Model (`models/seed.py`):** `order_matters` field updated to `bool = False`.

**Frontend (`api.js`, `SeedFinder.jsx`):**
- `startFinder` takes `orderMatters` as trailing arg, passes `!!orderMatters` through.
- New `const [orderMatters, setOrderMatters] = useState(false)`.
- Toggle `<Field label="Order Matters?"><Seg options={["No - Any Order", "Yes - Exact Order"]} …/>` rendered when `!isWhiteMikey`, disabled while running.
- On rush change to White/Mikey: `setOrderMatters(false)` (with other White-only resets). Violet/Red/Yellow switches preserve value.
- `handleStart` passes `orderMatters` to `startFinder`.

**Tests:** 4 new in `test_bridge.py` (White/Mikey rejection, Violet acceptance, exact-positions end-to-end, default-off baseline). **pytest: 35 passed**, `npm run build` clean.

## Player Lookup — Medals toggle + Text size toggle

**Bridge (`bridge.py`):**
- Dropped `rush_key` parameter from `_resolve_level_code` and `_get_medal` — it was never read; level codes are globally unique. Updated the one caller in `calculate_timer`.
- `run_player_lookup` row emit now includes `medal: str` computed via `_get_medal(display, entry.score / 1000.0)`.

**Frontend (`shared.jsx`):**
- `MedalBadge` extended with `plain` prop (default `false`). When `true`: colored text only (no pill/border/background), `fontSize: "0.82em"`, `fontWeight: 700`. Run Timer's existing usage unchanged.

**Frontend (`PlayerLookup.jsx`):**
- Imports `MedalBadge`, `MedalToggle` from `shared.jsx`.
- Added `showMedals` (default false) and `largeText` (default false) local state.
- Results pane header: `<MedalToggle>` + Normal/Large `<Seg>` appear once rows are loaded; player name moved inline.
- Conditional 5th "Medal" `<th>`/`<td>` — column appears/disappears without layout break.
- Results table wrapped in `<div style={{ fontSize: largeText ? 14 : 11 }}>`. `TH`/`TD` constants converted to relative `em` units so they scale with the wrapper.

**Tests:** 2 new in `tests/test_bridge.py` — `test_get_medal_movement_ace` (Movement @ 24.0s → "ACE") and `test_player_lookup_rows_include_medal` (medal field shape). **pytest: 37 passed**, `npm run build` clean.

## Button standardization (Part 1) — Global Export, Level Search, Player Lookup

Moved the Run/Stop button pair from PageHead `actions` to the bottom of each page's `.form` panel, matching the Rush Tools pattern. Copy button stays top-right. Bump to `size="lg"`. ~5-line diff per file. No state changes, no new imports.

**Files:** `GlobalExport.jsx`, `LevelSearch.jsx`, `PlayerLookup.jsx`.

## Compare Players page (Part 2)

New page under Leaderboard Tools for side-by-side time comparison of two Steam IDs.

**Bridge (`bridge.py`):**
- Added `run_compare_players(steam_id_1, steam_id_2, mode, target)`.
- ID validation (17-digit check) runs before the Steam-ready check so tests can exercise it without a live Steam session.
- One `find_leaderboard` call per level; two `get_player_entry` calls against the shared handle — ~3N Steam calls (not 6N).
- Emits `_nwCompareEvent` with status / row / done events. Row event carries `p1`, `p2` (each `{rank, time, score_ms, medal}` or `null`), `delta_ms`, `faster` ("p1"/"p2"/"tie"/null), `total`.

**Frontend (`api.js`):** `runComparePlayers(sid1, sid2, mode, target)` wrapper.

**Frontend (`shared.jsx`):** Added `{ key: "compare", label: "Compare Players", icn: "user" }` to `NAV_ITEMS.leaderboard`.

**Frontend (`main.jsx`):** Imported `ComparePlayers`, added to `LB_PAGES` and `PAGE_TITLES`.

**New page (`ComparePlayers.jsx`):**
- Two Steam ID fields, each with a "Mine" button.
- Mode Seg (level/chapter/game) + level/chapter dropdowns matching PlayerLookup.
- Run/Stop at bottom of form.
- Results table: Level | P1 Rank | P1 Time | [P1 Medal] | Δ | [P2 Medal] | P2 Time | P2 Rank.
- Faster Time cell highlighted (`rgba(120,220,160,0.12)`); Δ signed seconds, 3 dec, Unicode minus, colored green when a winner exists.
- MedalToggle + Normal/Large Seg in results pane header.
- Copy: tab-separated with both player names in header row.

**Tests:** `test_run_compare_players_validates_both_steam_ids` covers bad sid1 and bad sid2. **pytest: 38 passed**, `npm run build` clean.

## Compare Players: CSV export

Extended `run_compare_players` with `out_mode` and `folder` params (same pattern as Player Lookup). CSV columns: `level, p1_rank, p1_time, p1_medal, delta_ms, p2_rank, p2_time, p2_medal`. Filename: `P1_vs_P2_context.csv`. Done event includes `csv_path`.

Frontend `ComparePlayers.jsx`: added `outMode` Seg, conditional folder Field with Browse button, `folderTouched` guard, Copy hidden when csv-only, csv-only right-panel state. `runComparePlayers` in `api.js` takes `outMode`/`folder` trailing args.

`npm run build` clean, 38 tests passing.

## What's next

- Ghosts sheet still has placeholder ID in `resources.py`.
- `neonwhite.spec` cleanup (drop credentials from bundle).
- ~~In-app smoke of Compare Players CSV export~~ ✓ All three output modes verified (display / csv / both). CSV columns correct, filename correct, Copy/table visibility correct per mode.
- Ghosts sheet still has placeholder ID in `resources.py`.
- `neonwhite.spec` cleanup (drop credentials from bundle).

## Player Lookup — Avg Placement toggle

- `frontend/src/shared.jsx`: added `AvgPlacementToggle` export alongside `MedalToggle` (same iOS switch pattern, label "Avg Placement").
- `frontend/src/pages/PlayerLookup.jsx`: imported `AvgPlacementToggle`; added `showAvg` + `totalLevels` state; `totalLevels` set from `done.total_levels`, reset on `handleRun`; toggle rendered in results-pane header; footer conditionally shown post-done (`showAvg && !running && rows.length > 0`) with `#avgRank across X / totalLevels levels`, font scales with `largeText`. No bridge changes.
- Build clean; 38 pytest passing.

## GitHub hosting + update workflow — plan only (paused)

Discussed end-to-end pipeline for hosting on public GitHub, CI-built EXE on tag push, and in-app version-check banner (option 2; rejected option 3 full auto-update for one-file PyInstaller cost reasons).

**Scope decisions:**
- Don't bundle `credentials.json` (OAuth client secret risk in public Release).
- Don't bundle `steam_api64.dll` (Steamworks redistribution; document instead).
- Confirmed: `webview_app/main.py` is the shipping target going forward; `neonwhite_app.py` archived. `APP_VERSION` in `bridge.py:28` becomes the source of truth.

**Discovered:**
- React frontend has zero Sheets references — dropping `credentials.json` from the bundle has no user-visible impact in the shipping app.
- Spec entry point currently still `neonwhite_app.py`; needs switch to `webview_app/main.py` plus `frontend/dist` data add.
- `open_external_url` allow-list (`bridge.py:1181`) only permits Drive/Docs/YouTube — needs `https://github.com/` for the Releases link.

**Saved plan:** `00_Inbox/plans.md` (top entry) + `~/.claude/plans/can-we-talk-through-steady-russell.md`.

**Memory updates:**
- New `feedback_distribution_secrets.md` — no secrets/3rd-party DLLs in distributed builds.
- New `project_app_target.md` — webview_app/main.py is canonical; neonwhite_app.py archived.

User paused before any code changes. No files in `SteamScraper/` or `frontend/` modified this session.

---

# Session 2026-05-08

## Seed Finder — copy-seed button per result card (from prior session)

Added a small "copy" button inline on each `SeedCard` header row. Clicking writes `result.seed` to clipboard; flashes "✓" for 1.5s. Commit `286bad0`.

---

## Compare Players — P1/P2 colored highlights

Replaced the single green `FASTER_BG/COLOR` pair with per-player colors: P1 blue (`rgba(80,160,255,0.18)` / `rgb(80,160,255)`) and P2 red (`rgba(255,90,90,0.18)` / `rgb(255,90,90)`). Delta column takes the winner's color. Commits `0a29b5b`, `fea28f0`.

## Cheater filtering — webview app

`fetch_cheater_list()` was never called in the webview app, leaving `cheater_ids` always empty (filtering silently disabled). Fixed:

- `bridge.py`: kick background thread calling `steam_api.fetch_cheater_list()` after Steam init succeeds; new `get_cheater_count()` bridge method.
- `api.js`: `getCheaterCount()` wrapper.
- `GlobalExport.jsx` + `LevelSearch.jsx`: "N cheaters filtered" label in accent color, top-right of results pane header. Fetched on mount and refreshed on `done` event. Commit `69deae4`.

## Todo cleanup

- Cheater list lookup (todo item) confirmed already a `set` — no fix needed, marked done.

Added a small "copy" button inline on each `SeedCard` header row, between the summary text and the expand ▲/▼ toggle.

- Clicking writes `result.seed` to clipboard via `navigator.clipboard.writeText`.
- Button flashes "✓" (accent color) for 1.5 s, then resets to "copy".
- Click is stopped from propagating so it doesn't toggle the card open/close.
- No new dependencies; styled with existing CSS vars.

**Commit:** `286bad0` — `frontend/src/pages/SeedFinder.jsx`, +23 lines. Build clean.

## World Record VODs — accent on "RECORD"

`PageHead` only supports a trailing `accentWord`, so passed a JSX node as `title` instead: `<>WORLD <span className="accent">RECORD</span> VODs</>`. Matches the accent pattern used on other pages.

**Commit:** `7375aab` — `frontend/src/pages/WorldRecordVods.jsx`, 1-line change. Build clean.
