# ── Secrets / credentials ─────────────────────────────────────────────
# Never commit these. They're OAuth client secrets and cached user tokens.
SteamScraper/credentials.json
SteamScraper/credentials.json.old
SteamScraper/credentials.json.old2
SteamScraper/token.json
SteamScraper/tokenold.json

# ── User config (per-machine paths, Sheet IDs) ────────────────────────
# Track neonwhite_config.example.json as a sanitized template instead.
SteamScraper/neonwhite_config.json
neonwhite_config.json

# ── Third-party redistributables ──────────────────────────────────────
# Valve's Steam runtime DLL — users should supply their own from their
# Neon White install via neonwhite_config.json's dll_path.
SteamScraper/steam_api64.dll

# ── Claude Code local-only settings ───────────────────────────────────
# Permission allowlists granted on this machine. A shared .claude/settings.json
# (without `.local`) IS meant to be tracked if you ever add one.
.claude/settings.local.json
SteamScraper/.claude/
steam_appid.txt

# ── Python ────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
.pytest_cache/

# ── PyInstaller build artifacts ───────────────────────────────────────
SteamScraper/build/
SteamScraper/dist/
*.manifest
NeonWhiteLeaderboardTool.exe
SteamScraper/NeonWhiteLeaderboardTool.exe

# Release zips and notes (built locally; uploaded to GitHub Releases instead)
NeonWhiteLeaderboardTool-*.zip
release-notes.md
Previous Versions/

# ── Compiled C side-products (keep shuffle.dll itself) ────────────────
# shuffle.dll is committed so the app runs out-of-box without a C compiler.
# These are intermediate MSVC linker outputs from compile_shuffle.py.
SteamScraper/shuffle.exp
SteamScraper/shuffle.lib
SteamScraper/shuffle.obj

# ── CSV outputs (sample/test data, regenerable) ───────────────────────
SteamScraper/*.csv

# ── Obsidian vault — local working notes, not part of public repo ─────
00_Inbox/
01_Codebase_Map/
02_Decisions/
03_Sessions/
plans/
.obsidian/
CLAUDE.md

# ── Loose local-only files ────────────────────────────────────────────
scraps/
uploads/
ClaudeDesignHandoff/
fonts/
tests/Ghosts Upload Log.md
i-m-configuring-a-frontend-declarative-clover.md

# ── OS / editor cruft ─────────────────────────────────────────────────
Thumbs.db
desktop.ini
.DS_Store
*.swp
*.swo
.vscode/
.idea/

# ── Logs ──────────────────────────────────────────────────────────────
*.log
SteamScraper/logs/

# -- Frontend build artifacts --
node_modules/
frontend/dist/

# ── tools/ dev scripts ────────────────────────────────────────────────────────
tools/.ghosts_token.json
tools/__pycache__/

# ── Legacy tkinter app (untracked; files kept on disk for reference) ──────────
SteamScraper/Neon White App versions/
SteamScraper/neonwhite_app.py
SteamScraper/tab_*.py
SteamScraper/leaderboard.py
SteamScraper/discover.py
SteamScraper/testingBalloon.py
SteamScraper/test_sheets_write.py

# ── Standalone scripts and dev probes (not part of live app) ─────────────────
SteamScraper/IL_level_search.py
SteamScraper/_seed_algo_probe.py
SteamScraper/_seed_finder_test.py
SteamScraper/_seed_ground_truth.py
SteamScraper/fullGlobalTop1000.py
SteamScraper/globalLeaderboard.py
SteamScraper/movement_leaderboard.py
SteamScraper/neonwhite_player_lookup.py
SteamScraper/neonwhite_player_lookup-v2.py
SteamScraper/sheets.py
SteamScraper/rthook_google.py
tools/build_ghosts_sheet.py

# ── SteamScraper untracked loose files ───────────────────────────────────────
SteamScraper/_seed_repro.py
